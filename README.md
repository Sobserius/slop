```console
git clone https://github.com/Sobserius/slop.git

cd slop

cp -r fuzzel ~/.config/
cp -r kitty ~/.config/
cp -r mako ~/.config/
cp -r waybar ~/.config/
cp -r mango ~/.config/
cp -r wallpaper.png ~/

cd
sudo rm -rf ~/slop
```
